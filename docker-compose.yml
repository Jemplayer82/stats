services:
  stats:
    build: .
    image: stats:latest
    ports:
      - "5000:5000"
    environment:
      - DATABASE_URL=sqlite:////data/usage.db
    volumes:
      - /storage/stats:/data
    restart: unless-stopped
